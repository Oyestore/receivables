import React from 'react';
import { Box, Typography } from '@mui/material';

const Settings: React.FC = () => (
    <Box sx={{ p: 3 }}>
        <Typography variant="h4">Accountant Settings</Typography>
        <Typography>Settings view placeholder.</Typography>
    </Box>
);

export default Settings;
