import React from 'react';
import { Box, Typography } from '@mui/material';

const Calendar: React.FC = () => (
    <Box sx={{ p: 3 }}>
        <Typography variant="h4">Calendar</Typography>
        <Typography>Calendar view placeholder.</Typography>
    </Box>
);

export default Calendar;
