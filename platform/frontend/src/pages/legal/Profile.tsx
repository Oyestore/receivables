import React from 'react';
import { Box, Typography } from '@mui/material';

const Profile: React.FC = () => (
    <Box sx={{ p: 3 }}>
        <Typography variant="h4">Profile</Typography>
        <Typography>Profile view placeholder.</Typography>
    </Box>
);

export default Profile;
