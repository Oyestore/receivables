import React from 'react';
import { Box, Typography } from '@mui/material';

const Analytics: React.FC = () => (
    <Box sx={{ p: 3 }}>
        <Typography variant="h4">Cash Flow Analytics</Typography>
        <Typography>Charts and forecasting will appear here.</Typography>
    </Box>
);

export default Analytics;
