import React from 'react';
import { Box, Typography } from '@mui/material';

const SystemLogs: React.FC = () => (
    <Box sx={{ p: 3 }}>
        <Typography variant="h4">System Logs</Typography>
        <Typography>Logs view placeholder.</Typography>
    </Box>
);

export default SystemLogs;
